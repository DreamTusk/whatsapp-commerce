meta {
  name: Update Status
  type: http
  seq: 2
}

put {
  url: {{base_url}}/api/users/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "is_active": false
  }
}
