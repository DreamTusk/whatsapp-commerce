meta {
  name: Get Shipment
  type: http
  seq: 2
}

get {
  url: {{base_url_local}}/shipments/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:path {
  id: <shipment_id>
}

settings {
  encodeUrl: true
  timeout: 0
}
