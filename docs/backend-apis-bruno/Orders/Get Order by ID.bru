meta {
  name: Get Order by ID
  type: http
  seq: 2
}

get {
  url: {{base_url_local}}/orders/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:path {
  id: <order_id>
}

settings {
  encodeUrl: true
  timeout: 0
}
