meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{base_url_local}}/storefront/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customer_access_token}}
}

headers {
  x-store-domain: freshmart.localhost
}

settings {
  encodeUrl: true
  timeout: 0
}
