meta {
  name: Clear Cart
  type: http
  seq: 5
}

delete {
  url: {{base_url_local}}/storefront/cart
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customer_access_token}}
}

headers {
  x-store-domain: freshmart.localhost
}

settings {
  encodeUrl: true
  timeout: 0
}
