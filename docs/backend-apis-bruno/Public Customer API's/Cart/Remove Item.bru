meta {
  name: Remove Item
  type: http
  seq: 4
}

delete {
  url: {{base_url_local}}/storefront/cart/:productId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{customer_access_token}}
}

headers {
  x-store-domain: freshmart.localhost
}

params:path {
  productId: <product_id>
}

settings {
  encodeUrl: true
  timeout: 0
}
