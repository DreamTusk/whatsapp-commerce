meta {
  name: Get Endpoint Request Error Rate Metric
  type: http
  seq: 3
}

get {
  url: https://graph.facebook.com/{{Version}}/{{Flow-ID}}?fields=metric.name(ENDPOINT_REQUEST_ERROR_RATE).granularity(LIFETIME).since(2024-01-28).until(2024-01-30)
  body: none
  auth: none
}

params:query {
  fields: metric.name(ENDPOINT_REQUEST_ERROR_RATE).granularity(LIFETIME).since(2024-01-28).until(2024-01-30)
}

docs {
  Can request specific fields by enabling the \`fields\` query param
}